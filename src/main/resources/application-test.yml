spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-poet.us.auth0.com/
  datasource:
    url: jdbc:h2:mem:poet;NON_KEYWORDS=user,value;MODE=MYSQL
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
management:
  endpoints:
    web:
      exposure:
        include: mappings
auth0:
  audience: poet-api-audience
  nickname: https://poet.jax.org/nickname
  email: https://poet.jax.org/email
api:
  version: /api/v1
initializePoet: false
releasekey: release
